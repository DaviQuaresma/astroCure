import ProfileLogTimeline from "./ProfileLogTimeline";

export const metadata = {
  title: "Logs do Perfil",
  description: "Visualização completa dos logs de atividades do perfil.",
};

export default function LogPanelPage() {
  return <ProfileLogTimeline />;
}
